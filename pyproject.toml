[project]
name = "smart-monsoon-farm"
version = "0.1.0"
description = "OpenEnv Smart Monsoon Farm"
authors = [{name = "Your Name"}]

dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:app"