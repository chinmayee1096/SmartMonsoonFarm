[project]
name = "smart-monsoon-farm"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[tool.openenv]
entrypoint = "server.app:main"